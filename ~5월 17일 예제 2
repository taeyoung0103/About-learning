(팀원들과 직접 만든 문제들입니다.)

점수문제
  시험 성적을 구분하고자 한다.
  91~100 = A, 81~90 = B, 71~ 80 = C, 61~70 = D, 그 이하는 F로 성적을 나누고자한다.
 
    s = input("Enter your Score : ")            # s라는 변수를 입력하는데, 입력하는 칸 앞에 "Enter your Score" 라는 구문이 같이 출력된다.
    s = int(s)                                  # s라는 변수를 입력하는데, 이 변수는 정수이여야 한다.

    def rank(s):                                # rank라는 함수를 만드는데,
      if 91 <= s <= 100 :                       # 만약 변수 s가 91 이상 100 이하인 구간에 속한다면
        return "A"                              # "A" 를 출력한다.
      elif 81 <= s < 91 :                       # 위 조건을 만족하지 않고 변수 s가 81 이상 91 미만인 구간에 속한다면
        return "B"                              # "B" 를 출력한다.
      elif 71 <= s < 81 :                       # 위 조건들을 만족하지 않고 변수 s가 71 이상 81 미만인 구간에 속한다면
        return "C"                              # "C" 를 출력한다.
      elif 61 <= s < 71 :                       # 위 조건들을 만족하지 않고 변수 s가 61 이상 71 미만인 구간에 속한다면
        return "D"                              # "D" 를 출력한다.
      else :                                    # 위 조건들을 모두 만족하지 않는다면 (즉, 61 미만이라면)
        return "F"                              # "F" 를 출력한다.

     예를 들어 print(rank(82)) 를 실행하면 "B" 가 출력된다.
     좀더 출력구문을 꾸미고 싶을떄, print("rank is : ", rank(82)) 를 실행하면, rank is B 가 출력될것이다.
     
     
     
     
체감온도와 실제측정온도 (온도 문제)
  체감온도가 실제측정온도 - 0.5인 값보다 낮을경우 "실제보다 시원하다고 느낀다." 를 출력하고, 체감온도가 실제측정온도 + 0.5인 값보다 높을경우 "실제보다 덥다고 느낀다." 를 출력한다.
  실제측정온도 - 0.5 <= 체감온도 <= 실제측정온도 + 0.5 인 경우 "적절히 느끼고 있다." 를 출력한다.
  
  
    a = float(input("체감온도 : "))              # a값을 입력하라고 표시되는데, 이 a값은 실수이며, 값 입력칸에 "체감온도 : " 라는 구문이 추가된다.
    b = float(input("실제측정온도 : "))           # b값을 입력하라고 표시되는데, 이 b값은 실수이며, 값 입력칸에 "실제측정온도 : " 라는 구문이 추가된다.

      def pa(a, b):                               # pa 라는 함수를 작성하며, 이 함수는 (a, b) 2개의 변수를 필요로 한다.
        if a < b-0.5 :                            # a 값이 b - 0.5 한 값보다 작을 경우 
          return "실제보다 시원하다고 느낀다."      # "실제보다 시원하다고 느낀다." 를 출력한다.
        elif a > b + 0.5 :                        # a 값이 b + 0.5 한 값보다 클 경우
          return "실제보다 덥다고 느낀다."          # "실제보다 덥다고 느낀다." 를 출력한다.
        else :                                    # 위 조건들을 모두 만족하지 않을 경우
          return "적절히 느끼고 있다."              # "적절히 느끼고 있다." 를 출력한다.
          
     작성한 것을 실행할 경우 
      1번쨰로 "체감온도 : " 를 입력하라고 나온다.     
      2번째로 "실제측정온도 : " 를 입력하라고 나온다.
      만약 33.0, 35.5를 차례로 입력한 경우 "실제보다 시원하다고 느낀다." 가 출력된다.
      
      
      
 
 (다른 팀 작성문제입니다.)
 
 카드 승부 문제
  1부터 100까지의 숫자카드가 있다. 두사람 a,b 가 각각 1장의 카드를 뽑아서 a가 뽑은 숫자가 b보다 크면 "a가 승리했습니다." 를 출력, b가 뽑은 숫자가 a보다 크면 "b가 승리했습니다." 를 출력,
  a가 뽑은 카드와 b가 뽑은 카드가 같을경우 "무승부입니다." 를 출력하고자 한다.
  
    a = input("a가 뽑은 수는 : ")                    # a 값을 입력하라는 칸이 나오며, 입력 칸 앞에 "a가 뽑은 수는 : " 이라는 구문이 추가되어 나온다.
    a = int(a)                                      # a 값은 정수이여야 한다.
    b = input("b가 뽑은 수는 : ")                    # b 값을 입력하라는 칸이 나오며, 입력 칸 앞에 "b가 뽑은 수는 : " 이라는 구문이 추가되어 나온다.
    b = int(b)                                      # b 값은 정수이여야 한다.

    def vs(a, b) :                                  # 함수 vs를 만들며, 변수를 2개 입력하여야 한다.
      for d in range(100):                          # 1 ~ 100 이라는 범위를 조건으로 한다.
        if a > b :                                  # 만약 a 가 b 보다 크다면
          return "A가 승리했습니다 "                 # "A가 승리했습니다" 라는 구문을 출력한다.
        elif a < b :                                # 만약 a 가 b 보다 작다면
          return "B가 승리했습니다."                 # "B가 승리했습니다" 라는 구문을 출력한다.
        else :                                      # 위 조건들을 모두 만족하지 않을 경우
          return "무승부입니다."                     # "무승부입니다." 를 출력한다.
        
    작성한 것을 실행할 경우 
      1번쨰로 "a가 뽑은 수는 : " 이라는 구문이 나오고, 그 뒤에 정수값을 입력하라는 빈칸이 나온다.
      2번쨰로 "b가 뽑은 수는 : " 이라는 구문이 나오고, 그 뒤에 정수값을 입력하라는 빈칸이 나온다.
      만약 70, 35를 차례로 입력할 경우 "A가 승리했습니다." 가 출력된다.
      반대로 35, 70을 차례로 입력할 경우 "B가 승리했습니다." 가 출력된다.
      35, 35를 차례로 입력할 경우 "무승부입니다." 가 출력된다.
      
      
